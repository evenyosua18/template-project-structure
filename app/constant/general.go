package constant

//for storing constant value, you can create another file for easier to read

// layer
const (
	RepositoryLayer  = `Repository`
	InteractionLayer = `Interaction`
	ServiceLayer     = `Service`
)

// string boolean
const (
	True  = `true`
	False = `false`
)
